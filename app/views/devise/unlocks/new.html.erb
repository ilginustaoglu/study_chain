<div class="auth-container">
  <div class="auth-card">
    <h2>Unlock Your Account</h2>
    <p class="auth-subtitle">Enter your email to receive unlock instructions</p>

    <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: 'auth-form' }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-group">
        <%= f.label :email, "Email Address" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control', placeholder: 'Enter your email' %>
      </div>

      <div class="form-actions">
        <%= f.submit "Resend Unlock Instructions", class: 'btn btn-primary btn-block' %>
      </div>
    <% end %>

    <div class="auth-links">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
